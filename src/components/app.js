import React, { Component } from 'react';

import StreamContainer from '../containers/container';

export default class App extends Component {
	render() {
		return (
			<div>
				<StreamContainer />
			</div>
		);
	}
}